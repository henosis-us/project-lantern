# Lantern Media Server (FastAPI) - example environment
# Copy to `.env` and adjust as needed.

# Where the media server considers itself reachable.
# This is what gets stored in the Identity Service when the server heartbeats.
LMS_PUBLIC_URL=http://localhost:8000

# Where the Identity Service is running locally.
IDENTITY_SERVICE_URL=http://localhost:8001

# Media server SQLite DB location
DATABASE_PATH=data/lantern.db

# CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173

# Optional
LOG_LEVEL=INFO

# Optional: TMDb key for metadata enrichment
# TMDB_API_KEY=
